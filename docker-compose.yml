version: '3.7'
services:
 db:
   image: postgres:latest
   ports:
     - '5432:5432'
   volumes:
     - ./data:/var/lib/postgres/data

   environment:
     POSTGRES_DB: app
     POSTGRES_USER: postgres
     POSTGRES_PASSWORD: example